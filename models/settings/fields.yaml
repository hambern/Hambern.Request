fields:
    send_mail:
        label: 'hambern.request::lang.labels.send_mail'
        type: switch
        default: false
    receive_groups:
        label: 'hambern.request::lang.labels.receive_groups'
        type: checkboxlist
    default_status:
        label: 'hambern.request::lang.labels.default_status'
        type: dropdown
        emptyOption: —
